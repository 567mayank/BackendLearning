<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white m-10">
    <a href="/" class="text-blue-400">Back</a>
    <h1 class="text-2xl mb-4 mt-4">All Users</h1>
    <div class="flex flex-wrap gap-x-8 gap-y-4">
        <% Users.forEach((user)=>{ %>
            <div class=" w-52 bg-zinc-800 rounded-lg p-2">
                <div class="mb-1 overflow-hidden">
                    <img class="w-52 h-52 object-cover object-top" src="<%=user.image%>" alt="image">
                </div>
                <div class="text-xl"><%=user.name%></div>
                <div class="text-zinc-500 mb-1"><%=user.email%></div>
                <a class="text-yellow-500 mr-6" href="/edit/<%=user.id%>">Edit</a>
                <a class="text-red-400" href="/delete/<%=user.id%>">Delete</a>
            </div>
        <% }) %>
    </div>
</body>
</html>